import { NgxNullablePipe } from './ngx-nullable.pipe';

describe('NgxNullablePipe', () => {
  it('create an instance', () => {
    const pipe = new NgxNullablePipe();
    expect(pipe).toBeTruthy();
  });
});
